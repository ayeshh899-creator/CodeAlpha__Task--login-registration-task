# Login and Registration System

A C++ program with user registration and login functionality. User credentials are stored in a text file (`users.txt`).

## Features
- User registration with username and password
- Login verification against stored credentials
- Data stored in `users.txt` file
- Simple menu-driven interface
